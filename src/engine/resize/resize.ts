export function resize(
	_w: number,
	_h: number,
	minWidth: number,
	minHeight: number,
) {
	// if (w < h) {
	return { width: minWidth, height: minHeight };
	// } else {
	// 	return { width: minHeight, height: minWidth };
	// }
}
